<?xml version="1.0"?>
<ruleset name="TrainRouting">
    <description>PSR-12 coding standard for Train Routing Backend</description>

    <!-- Use PSR-12 as base -->
    <rule ref="PSR12"/>

    <!-- Paths to check -->
    <file>src</file>
    <file>tests</file>

    <!-- Exclude patterns -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/var/*</exclude-pattern>
    <exclude-pattern>*/config/*</exclude-pattern>

    <!-- Show progress -->
    <arg value="p"/>

    <!-- Use colors -->
    <arg name="colors"/>

    <!-- Show sniff codes in reports -->
    <arg value="s"/>

    <!-- Check PHP files only -->
    <arg name="extensions" value="php"/>
</ruleset>
